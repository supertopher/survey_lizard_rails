<p>
  <%= f.label :content, "Question" %><br />
  <%= f.text_area :name, :rows => 3 %><br />
</p>
<%=  %>
<%= f.fields_for :choices do |answer| %>
  <%= render 'choices_fields', :f => answer %>
<% end %>

